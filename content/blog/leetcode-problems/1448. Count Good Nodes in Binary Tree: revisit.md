---
title: "1448. Count Good Nodes in Binary Tree: revisit"
date: 2026-05-03
tags: ["neetcode150", "tree", "python"]
---

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def goodNodes(self, root: TreeNode) -> int:
        # bfs traversal...?
        # dfs is more intuitive (carry current max along param)

        # output: number of nodes >= current_max
        def dfs(node: TreeNode, current_max: int) -> int:
            if not node:
                return 0
            node_is_good = node.val >= current_max
            current_max = max(current_max, node.val)
            return (
                dfs(node.left, current_max)
                + dfs(node.right, current_max)
                + int(node_is_good)
            )

        return dfs(root, root.val)
```
