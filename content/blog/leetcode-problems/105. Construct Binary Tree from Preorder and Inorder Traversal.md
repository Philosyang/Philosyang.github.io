---
title: "105. Construct Binary Tree from Preorder and Inorder Traversal"
date: 2026-01-30
tags: ["neetcode150", "tree", "python"]
---

This is not the optimal answer - currently it runs O(n**2). I will implement the hashmap as well as no-list-copying tomorrow.

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:
        # idea:
        # divide and conquer,
        # recursive
        if not preorder:
            return

        def treeBuilder(node, pre_o: List[int], in_o: List[int]) -> None:
            if not len(pre_o):
                return

            node.val = pre_o[0]

            idx = in_o.index(pre_o[0])
            l_inorder, r_inorder = in_o[:idx], in_o[idx + 1 :]
            l_len = len(l_inorder)
            l_preorder, r_preorder = pre_o[1 : 1 + l_len], pre_o[1 + l_len :]
            if l_inorder:
                node.left = TreeNode()
                treeBuilder(node.left, l_preorder, l_inorder)
            if r_inorder:
                node.right = TreeNode()
                treeBuilder(node.right, r_preorder, r_inorder)

            return

        root = TreeNode()
        treeBuilder(root, preorder, inorder)

        return root
```
