---
title: "110. Balanced Binary Tree"
date: 2026-06-08
tags: ["neetcode150", "tree", "python"]
---

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right


# cases:
# not node -> return 0
# node -> if abs(left height - right height) > 1 then break False -> return max(left height, right height)

class Solution:
    def isBalanced(self, root: Optional[TreeNode]) -> bool:
        result = True

        def checkBalance(node: Optional[TreeNode]) -> int:
            nonlocal result
            if not node:
                return 0
            lh = checkBalance(node.left)
            rh = checkBalance(node.right)
            if abs(lh - rh) > 1:
                result = False

            return 1 + max(lh, rh)

        checkBalance(root)
        return result
```
