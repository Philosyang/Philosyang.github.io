---
title: "1448. Count Good Nodes in Binary Tree"
date: 2026-01-11
tags: ["neetcode150", "tree", "python"]
---

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def goodNodes(self, root: TreeNode) -> int:
        # bfs/dfs traversal with running_max_val

        if not root:
            return 0

        dq = deque([(root, root.val)])  # node, max_val_seen
        result = 0

        while dq:
            node, max_val = dq.popleft()
            nv = node.val
            if nv >= max_val:
                result += 1
                max_val = nv
            if node.left:
                dq.append((node.left, max_val))
            if node.right:
                dq.append((node.right, max_val))
        
        return result
```
