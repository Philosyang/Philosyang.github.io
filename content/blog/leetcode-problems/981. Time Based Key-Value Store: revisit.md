---
title: "981. Time Based Key-Value Store: revisit"
date: 2026-04-23
tags: ["neetcode150", "binary-search", "python"]
---

takeaway: the `key=` param for bisect.

```python
class TimeMap:

    def __init__(self):
        # data structure: key: [(timestamp, value), ...]
        self.time_map = defaultdict(list)

    def set(self, key: str, value: str, timestamp: int) -> None:
        self.time_map[key].append((timestamp, value))

    def get(self, key: str, timestamp: int) -> str:
        if key not in self.time_map:
            return ""
        else:
            idx = (
                bisect.bisect_right(self.time_map[key], timestamp, key=lambda x: x[0])
                - 1
            )
            if idx >= 0:
                return self.time_map[key][idx][1]
            return ""


# Your TimeMap object will be instantiated and called as such:
# obj = TimeMap()
# obj.set(key,value,timestamp)
# param_2 = obj.get(key,timestamp)
```
