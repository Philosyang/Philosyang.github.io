---
title: "572. Subtree of Another Tree"
date: 2026-07-21
tags: ["neetcode150", "tree", "python"]
---

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right

# recursion
# is a subtree when same value, same left (incl None), same right
class Solution:
    def isSameTree(self, r0: Optional[TreeNode], r1: Optional[TreeNode]) -> bool:
        if not r0 and not r1:
            return True
        elif r0 and r1:
            return (
                r0.val == r1.val
                and self.isSameTree(r0.left, r1.left)
                and self.isSameTree(r0.right, r1.right)
            )
        return False

    def isSubtree(self, root: Optional[TreeNode], subRoot: Optional[TreeNode]) -> bool:
        if not subRoot:
            return True
        if not root:
            return False
        return (
            self.isSameTree(root, subRoot)
            or self.isSubtree(root.left, subRoot)
            or self.isSubtree(root.right, subRoot)
        )
```
