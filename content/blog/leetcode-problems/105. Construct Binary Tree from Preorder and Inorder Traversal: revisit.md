---
title: "105. Construct Binary Tree from Preorder and Inorder Traversal: revisit"
date: 2026-02-01
tags: ["neetcode150", "tree", "hashing", "divide-and-conquer", "python"]
---

Wow, I'm so good at this!

```python
# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:
        n = len(preorder)

        inorder_dict = {}
        for idx, val in enumerate(inorder):
            inorder_dict[val] = idx

        def treeBuilder(node, pidx: int, iidx_left: int, iidx_right: int):
            if pidx >= n or pidx < 0:
                return None

            val = preorder[pidx]
            node.val = val
            iidx_target = inorder_dict[val]
            if iidx_target > iidx_left:  # if we still have things to the left
                node.left = TreeNode()
                treeBuilder(node.left, pidx + 1, iidx_left, iidx_target - 1)
            if iidx_target < iidx_right:  # to the right
                node.right = TreeNode()
                treeBuilder(node.right, pidx + (iidx_target - iidx_left + 1), iidx_target + 1, iidx_right,)

            return

        root = TreeNode()
        treeBuilder(root, 0, 0, n - 1)

        return root

```